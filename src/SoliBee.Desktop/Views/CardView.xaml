<UserControl
    x:Class="SoliBee.Desktop.Views.CardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SoliBee.Desktop.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    PointerPressed="CardView_PointerPressed">

    <Grid x:Name="CardRoot" Width="85" Height="125">
        <!-- Card Face (Face Up) -->
        <Border x:Name="CardFace" BorderBrush="DarkGray" BorderThickness="1" Background="White" CornerRadius="6">
            <Grid Padding="4">
                <TextBlock x:Name="RankText" Text="A" FontSize="16" FontWeight="Bold" HorizontalAlignment="Left" VerticalAlignment="Top" />
                <TextBlock x:Name="MiniSuitText" Text="♠" FontSize="12" HorizontalAlignment="Left" Margin="2,18,0,0" VerticalAlignment="Top" />
                
                <!-- Vector Suit Shape Center -->
                <Path x:Name="SuitVector" Stretch="Uniform" Margin="16" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
        </Border>

        <!-- Card Back (Face Down) -->
        <Border x:Name="CardBack" BorderBrush="White" BorderThickness="2" CornerRadius="6" Visibility="Collapsed">
            <Grid x:Name="CardBackVectorGrid" Padding="6">
                <!-- Programmatic geometric vector pattern for Themes -->
                <Path x:Name="CardBackPattern" Stroke="White" StrokeThickness="1.5" Opacity="0.75" Stretch="Fill">
                    <Path.RenderTransform>
                        <TransformGroup>
                            <ScaleTransform x:Name="CardBackScaleTransform" CenterX="36.5" CenterY="56.5" />
                            <TranslateTransform x:Name="CardBackTranslateTransform" />
                        </TransformGroup>
                    </Path.RenderTransform>
                </Path>
            </Grid>
        </Border>
    </Grid>
</UserControl>
