# Root-level ignores — mac/ and windows/ have their own more specific .gitignore files
.DS_Store
.build/
.swiftpm/

# iOS: Honeycomb.xcodeproj is generated from ios/project.yml — run `xcodegen generate` in ios/
ios/Honeycomb.xcodeproj/
ios/build/
